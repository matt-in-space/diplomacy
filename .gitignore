# TypeScript frontend build tooling and compiled output. The compiled
# output lives under web/static/js/ (picked up by web/assets.go's existing
# //go:embed static) so go:embed sees it, but it's generated by `npm run
# build` in frontend/, not committed.
frontend/node_modules/
frontend/dist-test/
web/static/js/
